# 31 — QoL fixes from external feedback

**Status**: In Progress

## Overview

Implement the QoL batch specced in `SPECS/31-qol-fixes-29-07-2026.md`. Four small user-facing fixes
plus a docs refresh — almost all "surface an existing capability", with one genuinely new piece (a
minimal toast system that item B needs).

- **A** — Timeline collapse/expand toggle must not move (put expand button at the same left cell).
- **C** — Timeline wheel: plain=lanes (unchanged), Shift+wheel=pan time (new), Ctrl/Cmd=zoom
  (unchanged) + make all three discoverable in HotkeysModal.
- **D** — Retire the ✨ `IconSparkles` / `IconWand`; shared `EFFECT_ICON` map; Accordion `icon` prop.
- **E** — Fix stale `CLAUDE.md` (docs only).
- **B** — Drop files anywhere → open ImportModal pre-staged; fix silent-reject + no-incremental-feedback
  latent bugs; new toast system.

Implementation order: **A → C → D → E → B** (cheapest/lowest-risk first; new component + event
plumbing last).

## Task Context

- **Spec**: `SPECS/31-qol-fixes-29-07-2026.md` — read it; every requirement is lettered/numbered there.
- **Verify policy** (`.claude/skills/verify/SKILL.md` + CLAUDE.md): static checks only — `npx tsc -b`.
  Do NOT run the dev server / browser. Hand the user a click-X-look-for-Y checklist. Items B and C are
  wheel/drag behaviours only real browser testing can confirm.
- **Key files & anchors** (from spec, verify line numbers before editing — the codebase may have moved):
  - A: collapsed bar `App.tsx` (~696-706, `justify-between`); collapse cell `Timeline.tsx` (~621-630,
    `GUTTER_WIDTH`=32, `RULER_HEIGHT`=24, `COLLAPSED_TIMELINE_HEIGHT`=32).
  - C: native wheel listener `Timeline.tsx:262-284`; add `Shift`-branch before the Ctrl/Cmd zoom
    branch. HotkeysModal `GROUPS` `HotkeysModal.tsx:14-63` (`{keys,label,desc}`, "Scroll" as a key).
  - D: `IconSparkles` in `LeftRail.tsx` (47, 164 `IconWand`), `Timeline.tsx` (647),
    `PropertiesPanel.tsx` (1009, 1557, 1558-1572 the 15 cards), `ContextToolbar.tsx` (77).
    `EFFECT_LABEL` at `PropertiesPanel.tsx:908-931`; LeftRail per-effect icons `LeftRail.tsx:133-160`;
    `PRESET_ICON` pattern `LeftRail.tsx:31`; `Accordion` `PropertiesPanel.tsx:1581`. `IconAdjustments`
    is TAKEN (`ContextToolbar.tsx:10`). Verified available: `IconFilters`, `IconEaseInOut`, `IconPrism`.
  - E: `CLAUDE.md` — remove `AnnotationTools.tsx`; add WebGL/regl (`glEffects.ts`) + per-pixel rule;
    22 effect kinds from `types.ts:256-266`; fix file map + Done/Planned list.
  - B: `ImportModal.tsx` (`addFiles` ~107-122 silent-reject; `addUrl` ~129-153 the loading-row pattern
    to mirror; props 18-23; `PendingAsset` 30-42; inner drop 411-413; window paste 156-188). `App.tsx`
    (`addObjects` 245-258; `handleImportProject` alert 212-220; ImportModal wiring 738-744). New toast:
    `useToasts` hook in `src/hooks/` + container in `App` (precedent: `mediaRegistry.ts`).

## Blockers/Issues

None currently.

## TODO

### A — collapse/expand toggle
[X] Restructure the collapsed bar so the expand button is a left-most 32px cell with the "Timeline"
    label to its right (same x as the collapse cell). Chevrons already correct (down/up); titles match.

### C — wheel navigation + zoom
[X] Add a `Shift`-branch to the wheel listener (before Ctrl/Cmd zoom): `preventDefault()`, normalise
    `deltaMode`, `scrollLeft += dominantDelta`. Plain-wheel early return + zoom body untouched.
[X] Add three rows to HotkeysModal (new "Timeline" group: Scroll=lanes, Shift+Scroll=time, Ctrl+Scroll=zoom).

### D — retire sparkles
[X] Swap `IconSparkles`→`IconFilters` on the 4 effects-identity surfaces; `IconWand`→`IconPrism`;
    ContextToolbar Animate→`IconEaseInOut`.
[X] New shared module `src/components/effectIcons.ts`: `EFFECT_ICON: Record<VideoEffectKind, TablerIcon>`
    (22 kinds, populated from LeftRail's per-effect choices); LeftRail + PropertiesPanel both consume it.
[X] Added optional `icon?` prop to `Accordion` (falls back to `SECTION_ICONS[title]`); 15 per-effect
    cards pass `icon={effectIcon(effect.kind)}`; dropped the 15 dead per-effect SECTION_ICONS entries.
[X] `grep -rn "IconSparkles\|IconWand" src/` returns nothing. `npx tsc -b` clean.

### E — docs
[X] Removed `AnnotationTools.tsx` refs (grep CLAUDE.md clean); zoom/effect creation now cites `LeftRail.tsx`.
[X] Documented WebGL/regl pipeline (`glEffects.ts`) + the per-pixel-effects-never-getImageData rule.
[X] Corrected effect-kind list to 22 (tiered) from `types.ts:256-266`.
[X] Updated file map (added LeftRail/ContextToolbar/TransportBar/HotkeysModal/useUiPrefs/glEffects +
    missing lib modules) + reconciled Done/Planned spec list (specs run 02-31).

### B — drop-to-import + toasts
[X] Toast system: `src/hooks/useToasts.ts` (module store + `useSyncExternalStore` hook, `pushToast`/
    `dismissToast`, auto-dismiss 5s, cap 4) + `src/components/Toasts.tsx` container (top-centre,
    non-blocking: container `pointer-events-none`, each toast `pointer-events-auto`). Rendered in App.
[X] Window-level drag handlers in `App` (drag-depth counter, `preventDefault` on dragover, `hasFiles`
    guard); full-window "Drop to import" overlay (hidden when modal open).
[X] On drop: partition via `isSupportedMediaFile` (new export in assetStore); all-unsupported → toast +
    no modal (B14); else stash supported + open modal + toast rejects naming them (B15).
[X] New `initialFiles?: File[]` prop on ImportModal (staged once per array identity → append when
    already open, B9). Cleared on modal close so "Add media" doesn't re-stage stale files.
[X] Fixed B8 inside `addFiles`: `status:'loading'` rows up front, resolve in place (mirrors `addUrl`).
[X] Routing unified in App's window handler; modal's inner drop is visual-only (no stopPropagation) so
    the overlay depth counter always balances (no stuck overlay, AC 11). `npx tsc -b` clean.
[ ] BROWSER-ONLY verification (drag/drop + toasts) — see checklist handed to user.

### Extras (post-spec, user request 2026-07-31)
[X] Timeline bars/lanes shrink when zoomed far out — `laneHeightFor(pps)` lerps 40→20px below 24 px/s;
    all lane-column heights (bars, gutter rows, add-lane rows, drag math) derive from it.
[X] Collapse animations: timeline area height-animated (transition suppressed during splitter resize);
    LeftRail asset pane width-animated (always mounted, clipped) so it slides open/closed.

### Global
[X] `npx tsc -b` clean (after every item).
[ ] Hand user a browser click-X-look-for-Y checklist.

## Work Log

[2026-07-31] Item A — collapse/expand toggle no longer moves.
- Files modified: src/components/App.tsx (collapsed bar → left 32px cell + label; was justify-between).

[2026-07-31] Item C — timeline wheel: added Shift+wheel horizontal pan; kept plain=lanes, Ctrl/Cmd=zoom.
- Files modified: src/components/Timeline.tsx (wheel listener restructured, Shift branch + comment),
  src/components/HotkeysModal.tsx (new "Timeline" group, 3 wheel rows).

[2026-07-31] Item D — retired ✨ IconSparkles / IconWand; one EFFECT_ICON source of truth.
- Files added: src/components/effectIcons.ts (EFFECT_ICON map, 22 kinds).
- Files modified: src/components/LeftRail.tsx (consume EFFECT_ICON; Effects section→IconFilters; preset
  fallback→IconPrism; pruned now-unused icon imports), src/components/Timeline.tsx (effects gutter
  →IconFilters), src/components/ContextToolbar.tsx (Animate→IconEaseInOut), src/components/PropertiesPanel.tsx
  (header badge + generic Effects card→IconFilters; Accordion `icon` prop + effectIcon() helper; 15
  cards pass per-kind icon; removed 15 dead SECTION_ICONS entries).
- Verified: grep IconSparkles/IconWand clean; npx tsc -b exit 0.

[2026-07-31] Item E — refreshed stale CLAUDE.md (docs only, verified against source).
- Files modified: CLAUDE.md (removed AnnotationTools refs → LeftRail; added WebGL/glEffects tier +
  per-pixel rule; 22 tiered effect kinds; file map rebuilt with real components/hooks/lib files;
  Done/Planned reconciled to specs 02-31).
- Note: actual `App.tsx` path is `src/components/App.tsx` (doc uses bare component filenames by convention).

[2026-07-31] Item B — drop files anywhere to import + new toast system + two latent import bugs fixed.
- Files added: src/hooks/useToasts.ts (module store + hook), src/components/Toasts.tsx (top-centre stack).
- Files modified: src/lib/assetStore.ts (isSupportedMediaFile export), src/components/ImportModal.tsx
  (initialFiles prop + staged-once effect; B8 loading-rows-up-front in addFiles; inner drop now
  visual-only), src/components/App.tsx (window drag/drop handlers + depth counter + overlay; routes
  supported files to modal, toasts rejects, skips modal when all unsupported; renders <Toasts/>;
  clears dropFiles on close).
- Design note: App's window handler is the single drop-routing owner; the modal's drop zone keeps only
  its highlight (does NOT stopPropagation) so the drag-depth overlay counter always balances.
- Verified: npx tsc -b exit 0. Drag/drop + toast behaviour needs real browser testing (checklist below).

[2026-07-31] Extras — zoom-scaled lane height + collapse/expand animations.
- Files modified: src/components/Timeline.tsx (laneHeightFor + MIN_LANE_HEIGHT/LANE_SHRINK_BELOW_PPS;
  body `laneHeight` drives trackHeight/laneToY/all bar & gutter heights; drag lane-delta uses it too),
  src/components/LeftRail.tsx (content pane always-mounted, width-animated w-52↔w-0, overflow clipped),
  src/components/App.tsx (timeline area wrapped in height-animated container; SPLITTER_HEIGHT const;
  isResizingTimeline flag suppresses the transition during splitter drags).
- Verified: npx tsc -b exit 0.
